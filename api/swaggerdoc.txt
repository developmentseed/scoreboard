swagger: "2.0"
info:
  description: "Placeholder text."
  version: "1.0.0"
  title: "Scoreboard"
  contact:
    email: "placeholderemail@developmentseed.org"
basePath: "/scoreboard/api"
tags:
- name: "users"
  description: "User info"
- name: "campaigns"
  description: "Campaign descriptions"
schemes:
- "https"
- "http"
paths:
  # Note: need to add in fragment below
  /users:
    get:
      parameters:
      - in: query
        name: page
        type: "integer"
        description: "Page of users to return (groups of 25)"
        required: true
        default: 1
      - in: query
        name: active
        type: "integer"
        description: "Indicator for whether to include only users who have been active in the last 6 months"
        required: true
        default: false
      - in: query
        name: sortType
        type: "string"
        enum: [Least recent, Most recent, Most total, Least total]
        description: "Sorted order of users"
        required: false
      responses:
        200:
          description: "Valid"
  /campaigns:
    get:
      produces:
        []
      responses:
        200:
          description: "Valid"