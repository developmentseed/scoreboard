version: 2
jobs:
  build:
    docker:
      - image: circleci/node:8.11
    steps:
      - checkout

      - restore_cache:
          keys:
            - scoreboard-{{ package.json }}-{{ api/package.json }}-{{ frontend/package.json }}

      - run:
          name: Install
          command: yarn 
      - run:
          name: Bootstrap
          command: yarn bootstrap
      - run:
          name: Lint
          command: yarn eslint

      - save_cache:
          key: scoreboard-{{ package.json }}-{{ api/package.json }}-{{ frontend/package.json }}
          paths:
            - ~/node_modules
            - ~/api/node_modules
            - ~/frontend/node_modules